export interface IEnvConfig {
  environment: string;
  inventory_api: string;
  inventory_workflow_api: string;
  portal_path: string;
  portal_sub_path: string;
  sub_path: string;
  render_qiankun: boolean;
}
